@page "/skills"
@using PortfolioWebsite.Services
@using PortfolioWebsite.Models
@inject PortfolioDataService PortfolioDataService

<PageTitle>Skills - @_portfolioData.Name</PageTitle>

<MudContainer MaxWidth="MaxWidth.Large" Class="pa-4">
    <BreadcrumbNav CurrentPage="Skills" />
    <div class="portfolio-container">
        <SkillsSection Skills="@_portfolioData.Skills" />
    </div>
</MudContainer>

@code {
    private PortfolioData _portfolioData = new();

    protected override void OnInitialized()
    {
        _portfolioData = PortfolioDataService.GetPortfolioData();
    }
}
